<Window x:Class="FooWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FooWpf.ViewModel" d:DataContext="{d:DesignInstance Type=local:ViewModelMain}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="\Styles\MainStyles.xaml"/>
                <ResourceDictionary Source="\Styles\DataTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Background="Violet" VerticalAlignment="Center" Height="32">
            <Button Command="{Binding UICommand}" Width="70" Height="25" VerticalAlignment="Center" Margin="5,5,5,5"
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}}}"
                        Content="Button">
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" Background="Aqua" VerticalAlignment="Center" Height="32">
            <TextBox Width="500"  Height="25" Text="{Binding TextEntry,Mode=TwoWay}" Margin="5,5,5,5"></TextBox>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="2" VerticalAlignment="Center">
            <Control Template="{StaticResource resultsviewer}"></Control>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="2" VerticalAlignment="Center">
            <Control Template="{StaticResource resultslistbox}"></Control>
        </StackPanel>
    </Grid>
</Window>
